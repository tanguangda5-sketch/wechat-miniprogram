<view class="page">
  <view class="ambient ambientLeft"></view>
  <view class="ambient ambientRight"></view>
  <view class="grain"></view>

  <view class="content">
    <view class="brandBlock">
      <image class="brandAvatar" src="/images/login-brand.png" mode="aspectFill"></image>
      <view class="brandName">{{text.brandName}}</view>
      <view class="brandTagline">{{text.tagline}}</view>
    </view>

    <view class="heroBlock">
      <view class="heroTitle">{{hasWechatUser ? text.titleBindPhone : text.titleWechat}}</view>
      <view class="heroDesc" wx:if="{{!hasWechatUser}}">
        {{text.descBeforeLogin}}
      </view>
      <view class="heroDesc" wx:else>
        {{text.descAfterLogin}}
      </view>
    </view>

    <view class="formCard">
      <view class="cardTop">
        <view class="cardBadge">AgriTravel</view>
        <view class="cardHint">{{text.heroTitle}}</view>
      </view>

      <button
        wx:if="{{!hasWechatUser}}"
        class="primaryButton"
        bindtap="onWechatLogin"
        loading="{{loading}}"
        disabled="{{loading}}"
      >
        {{text.btnWechatLogin}}
      </button>

      <button
        wx:if="{{hasWechatUser}}"
        class="primaryButton"
        open-type="getPhoneNumber"
        bindgetphonenumber="onGetPhoneNumber"
        loading="{{loading}}"
        disabled="{{loading}}"
      >
        {{text.btnWechatPhone}}
      </button>

      <view class="agreement">{{text.agreementSummary}}</view>
    </view>
  </view>

  <view class="footer">{{text.footer}}</view>
</view>
